# ─── Production profile ───────────────────────────────────────
server:
  port: ${PORT:8080}

spring:
  jpa:
    show-sql: false

logging:
  level:
    com.assetops: INFO
    org.springframework: WARN

management:
  endpoint:
    health:
      show-details: when-authorized
